mxcp: 1
tool:
  name: test_integer_no_schema
  description: Test function without proper parameter schema
  language: python
  source:
    file: ../python/test_endpoints.py
  # Note: No parameters section - this might bypass validation
  return:
    type: object
