mxcp: 1
tool:
  name: count_item_keys_strict
  description: Count keys in each item (strict additionalProperties=false)
  language: python
  source:
    file: ../python/test_endpoints.py
  parameters:
    - name: items
      type: array
      items:
        type: object
        properties:
          objid:
            type: integer
            description: Object ID (objid).
        required: ["objid"]
        additionalProperties: false
  return:
    type: object
    properties:
      counts:
        type: array
        items:
          type: integer
    required: ["counts"]
