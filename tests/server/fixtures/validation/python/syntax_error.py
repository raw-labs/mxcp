"""Python file with syntax error for testing validation."""


def process_data(user_id: int, name: str) -> dict
    # Missing colon after function signature - syntax error
    return {"user_id": user_id, "name": name}

