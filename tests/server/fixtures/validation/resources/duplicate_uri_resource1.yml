mxcp: 1

resource:
  uri: test://duplicate.resource/{id}
  description: First resource with duplicate URI
  parameters:
    - name: id
      type: string
      description: Resource identifier
  return:
    type: object
    properties:
      id:
        type: string
      value:
        type: string
  source:
    code: |
      SELECT $id as id, 'value1' as value
