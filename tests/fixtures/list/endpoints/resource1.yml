mxcp: "1.0.0"

resource:
  name: resource1
  description: A simple test resource
  uri: "test://resource1"
  parameters:
    - name: id
      type: string
      description: Resource identifier
      format: uri
  source:
    code: |
      SELECT 1 as value