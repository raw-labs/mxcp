mxcp: "1.0.0"

tool:
  name: tool2
  description: A nested test tool
  parameters:
    - name: input
      type: string
      description: Input parameter
      minLength: 1
      maxLength: 100
  source:
    code: |
      SELECT 2 as value 