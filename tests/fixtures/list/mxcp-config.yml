mxcp: "1.0.0"
projects:
  test_project:
    profiles:
      test:
        secrets:
        - name: test_secret
          type: http
          parameters:
            EXTRA_HTTP_HEADERS:
              Authorization: "Bearer test_token" 