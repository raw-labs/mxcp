mxcp: "1.0.0"
projects:
  secrets:
    profiles:
      default:
        secrets:
          - name: test_secret
            type: http
            parameters:
              EXTRA_HTTP_HEADERS:
                header1: "Foo"
                header2: "Bar"