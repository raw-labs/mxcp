mxcp: 1
tool:
  name: strict_endpoint
  description: "Test endpoint with strict schema (additionalProperties: false)"
  return:
    type: object
    properties:
      name:
        type: string
      age:
        type: integer
    additionalProperties: false
  source:
    file: ../sql/allowed_columns.sql 