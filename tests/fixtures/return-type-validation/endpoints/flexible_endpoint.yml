raw: "1.0.0"
tool:
  name: flexible_endpoint
  description: "Test endpoint with flexible schema (additionalProperties: true)"
  return:
    type: object
    properties:
      name:
        type: string
      age:
        type: integer
    additionalProperties: true
  source:
    file: extra_column.sql 