mxcp: "1.0.0"
tool:
  name: strict_endpoint
  description: "Test endpoint with strict schema (additionalProperties: false)"
  return:
    type: object
    properties:
      name:
        type: string
      age:
        type: integer
    additionalProperties: false
  source:
    file: allowed_columns.sql 