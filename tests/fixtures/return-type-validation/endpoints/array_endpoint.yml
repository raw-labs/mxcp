raw: "1.0.0"

tool:
  name: array_endpoint
  description: Test endpoint that returns an array of objects
  parameters:
    - name: name
      type: string
      description: Name parameter
    - name: age
      type: integer
      description: Age parameter
  return:
    type: array
    items:
      type: object
      properties:
        name:
          type: string
        age:
          type: integer
  source: 
    file: array_endpoint.sql 